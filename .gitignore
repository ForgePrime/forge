*.pyc
__pycache__/
*.pyo
*.egg-info/
.ruff_cache/
.mypy_cache/

# Runtime output (per-project state, not tracked)
#forge_output/

# Test-only workspace artifacts that accumulate during pytest runs with
# workspace_infra. Real project workspaces (forge_output/<real-slug>/workspace/)
# stay tracked for developer continuity; the patterns below match only
# transient test-generated slugs (p1pause-*, p2ac-*, e2e-*, …).
forge_output/p1pause-*/
forge_output/p2ac-*/
forge_output/p3*-*/
forge_output/p5*-*/
forge_output/e2e-*/
forge_output/*/workspace/__pycache__/
forge_output/*/workspace/node_modules/
forge_output/*/workspace/.pytest_cache/

# Git worktrees for parallel task execution
forge_worktrees/

# User-specific config (template is tracked, actual file is not)
.claude/forge.local.md
.mcp.json

# Plugin registry (per-machine paths, rebuilt by scan)
forge_plugins.json

deep-process/

# Dependencies
node_modules/

# Next.js build
.next/
out/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

.documentation/

# Smoke-test artifacts (PLAN_PRE_FLIGHT Stage 0.3 — regenerated per run)
platform/smoke_results.json
platform/smoke_findings_to_create.jsonl

# Forge redesign bundle (large; lives on dev machine, referenced via design_canonical/README.md)
platform/docs/Forge redesign - standalone.html
platform/docs/Forge_redesign_template_unpacked.html

# Vendor JS extracted from the redesign bundle (React/ReactDOM/Babel runtime — not Forge IP).
# Forge-authored extracted modules (_application-javascript, _text-jsx, inline_babel) ARE tracked.
platform/docs/forge_redesign_extracted/b6264c9d_text-javascript.txt
platform/docs/forge_redesign_extracted/c63700f7_text-javascript.txt
platform/docs/forge_redesign_extracted/11200eec_text-javascript.txt

# Local DB backups (taken before destructive operations like Phase 1 migration).
# Stay on dev machine; never committed.
platform/backups/