"""Evidence + causal-graph module.

Phase B Stage B.1+ holds the DAG infrastructure:
- acyclicity.py — pre-insert check for CausalEdge ordering invariant.
- (B.3) causal_graph.py — query service over CausalEdge.
- (B.4) context_projector.py — projection of minimal justification frontier.

Per PLAN_MEMORY_CONTEXT.
"""
