# /onboard $ARGUMENTS

Onboard a brownfield project into Forge — discover existing knowledge, import decisions and conventions, configure gates.

Read and follow the procedure in `skills/onboard/SKILL.md`.

The project path is: $ARGUMENTS (if empty, use current working directory)
