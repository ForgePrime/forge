{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$TOOL_INPUT\" | grep -q \"core.pipeline complete \"; then echo \"Task completed. Run /status to verify pipeline state. Remember to record any unlogged changes or decisions.\"; fi'"
          }
        ]
      }
    ]
  }
}
